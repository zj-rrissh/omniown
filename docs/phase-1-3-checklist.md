# Phase 1-3 验收清单

## 环境准备

```bash
# 1. 构建 sidecar
./scripts/build-sidecar.sh

# 2. 确保 config 存在
cp config.example.toml config/omniown.toml  # 如不存在
```

## Phase 1: 托盘面板

| # | 测试项 | 预期 | 结果 |
|---|--------|------|:--:|
| 1.1 | `cargo tauri dev` 启动 | 仅托盘图标出现，无主窗口 | |
| 1.2 | 左键单击托盘图标 | 右下角弹出 400×600 无边框面板 | |
| 1.3 | 再次左键托盘图标 | 面板隐藏 | |
| 1.4 | 右键托盘 → "显示/隐藏" | 同左键 toggle | |
| 1.5 | 拖拽面板 header 区域 | 窗口可拖拽移动 | |
| 1.6 | 点击面板外部（桌面/其他窗口） | 面板自动隐藏 | |
| 1.7 | 面板隐藏后托盘图标仍存在 | 托盘图标不消失 | |
| 1.8 | 快速连续点击托盘 | 面板不闪烁，正常 toggle | |

## Phase 2: sidecar 集成

| # | 测试项 | 预期 | 结果 |
|---|--------|------|:--:|
| 2.1 | 启动后访问 http://127.0.0.1:17777 | 返回 UI 页面 | |
| 2.2 | 面板内搜索文档 | 能搜到已导入的文档 | |
| 2.3 | 右键 → 退出 | 托盘消失，进程退出，端口释放 | |
| 2.4 | 手动 kill sidecar (`kill <pid>`) | 自动重启，面板恢复可用 | |
| 2.5 | 重复 2.4 多次 | 每次都能自动重启 | |

## Phase 3: 配置界面 & 路由

| # | 测试项 | 预期 | 结果 |
|---|--------|------|:--:|
| 3.1 | 面板底栏点击"设置" | 跳转 ConfigView，看到表单 | |
| 3.2 | 底栏点击"状态" | 跳转 StatusView，看到统计 | |
| 3.3 | 底栏点击"搜索" | 回到搜索/文档列表页 | |
| 3.4 | ConfigView 初始加载 | 显示 config/omniown.toml 中已有的 API 配置 | |
| 3.5 | 修改 API key → 保存 | 显示 "✅ 已保存" | |
| 3.6 | 检查 config/omniown.toml | [ai] 节的 api_key 已更新 | |
| 3.7 | 检查原有其他节 ([paths], [worker]) | 未被删除或覆盖 | |
| 3.8 | 搜索页刷新 | API 状态正常（无 embedding 相关报错） | |
| 3.9 | StatusView 数据 | 显示文档总数/公开/私有/已索引 | |
| 3.10 | 浏览器直接访问面板（无 Tauri） | UI 正常加载，设置页不崩溃 | |

## 自动化测试

```bash
# 运行 Rust 单元测试
cargo test --manifest-path src-tauri/Cargo.toml 2>&1
```

预期输出：12 tests passed (config 序列化 + 读写 + 合并 + 边界条件)
